create or replace TABLE PRACTICE.ML.SCORES (
	UID VARCHAR(16777216) NOT NULL,
	SESSION_DATE DATE NOT NULL,
	MODEL_NAME VARCHAR(16777216),
	MODEL_VERSION VARCHAR(16777216),
	SCORE FLOAT,
	primary key (UID, SESSION_DATE)
)